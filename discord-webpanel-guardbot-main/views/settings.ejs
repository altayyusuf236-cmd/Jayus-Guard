<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Genel Ayarlar - Guard Bot</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary-color: #5865f2;
            --secondary-color: #2c2f33;
            --accent-color: #7289da;
            --success-color: #43b581;
            --danger-color: #f04747;
            --warning-color: #faa61a;
            --dark-bg: #1a1a1a;
            --card-bg: #2a2a2a;
            --text-primary: #ffffff;
            --text-secondary: #b9bbbe;
            --border-color: #404040;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background: linear-gradient(135deg, #0f0f23 0%, #1a1a2e 50%, #16213e 100%);
            color: var(--text-primary);
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            min-height: 100vh;
        }

        /* Sidebar */
        .sidebar {
            position: fixed;
            left: 0;
            top: 0;
            height: 100vh;
            width: 280px;
            background: rgba(26, 26, 26, 0.95);
            backdrop-filter: blur(10px);
            border-right: 1px solid var(--border-color);
            z-index: 1000;
            transition: transform 0.3s ease;
        }

        .sidebar-header {
            padding: 20px;
            border-bottom: 1px solid var(--border-color);
            text-align: center;
        }

        .bot-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            border: 3px solid var(--primary-color);
            margin-bottom: 10px;
        }

        .bot-name {
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--text-primary);
            margin-bottom: 5px;
        }

        .bot-status {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        .nav-menu {
            padding: 20px 0;
        }

        .nav-item {
            list-style: none;
        }

        .nav-link {
            display: flex;
            align-items: center;
            padding: 15px 25px;
            color: var(--text-secondary);
            text-decoration: none;
            transition: all 0.3s ease;
            border-left: 3px solid transparent;
        }

        .nav-link:hover {
            background: rgba(88, 101, 242, 0.1);
            color: var(--text-primary);
            border-left-color: var(--primary-color);
        }

        .nav-link.active {
            background: rgba(88, 101, 242, 0.2);
            color: var(--primary-color);
            border-left-color: var(--primary-color);
        }

        .nav-icon {
            width: 20px;
            margin-right: 12px;
            font-size: 1.1rem;
        }

        /* Main Content */
        .main-content {
            margin-left: 280px;
            padding: 20px;
            min-height: 100vh;
        }

        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            padding: 20px;
            background: rgba(42, 42, 42, 0.8);
            backdrop-filter: blur(10px);
            border-radius: 15px;
            border: 1px solid var(--border-color);
        }

        .page-title {
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(45deg, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .server-info {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .server-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            border: 2px solid var(--primary-color);
        }

        .server-details h4 {
            margin: 0;
            color: var(--text-primary);
        }

        .server-details p {
            margin: 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .user-actions {
            display: flex;
            align-items: center;
        }

        .logout-btn {
            display: flex;
            align-items: center;
            gap: 8px;
            padding: 10px 20px;
            background: linear-gradient(45deg, var(--danger-color), #d32f2f);
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .logout-btn:hover {
            background: linear-gradient(45deg, #d32f2f, var(--danger-color));
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(244, 71, 71, 0.3);
            color: white;
            text-decoration: none;
        }

        .logout-btn i {
            font-size: 1rem;
        }

        /* Settings Grid */
        .settings-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 30px;
        }

        .settings-card {
            background: rgba(42, 42, 42, 0.8);
            backdrop-filter: blur(10px);
            border: 1px solid var(--border-color);
            border-radius: 15px;
            padding: 25px;
        }

        .card-header {
            display: flex;
            align-items: center;
            margin-bottom: 25px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--border-color);
        }

        .card-icon {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 15px;
            font-size: 1.5rem;
            color: white;
        }

        .card-icon.logs {
            background: linear-gradient(45deg, var(--primary-color), #2d4a7a);
        }

        .card-icon.backup {
            background: linear-gradient(45deg, var(--success-color), #2d5a2d);
        }

        .card-icon.security {
            background: linear-gradient(45deg, var(--warning-color), #7a4a2d);
        }

        .card-icon.notifications {
            background: linear-gradient(45deg, var(--accent-color), #4a2d7a);
        }

        .card-title {
            font-size: 1.3rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .setting-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 15px 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .setting-item:last-child {
            border-bottom: none;
        }

        .setting-info {
            flex: 1;
        }

        .setting-label {
            font-weight: 600;
            color: var(--text-primary);
            margin-bottom: 5px;
        }

        .setting-description {
            font-size: 0.9rem;
            color: var(--text-secondary);
        }

        /* Toggle Switch */
        .toggle-switch {
            position: relative;
            width: 60px;
            height: 30px;
        }

        .toggle-switch input {
            opacity: 0;
            width: 0;
            height: 0;
        }

        .slider {
            position: absolute;
            cursor: pointer;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-color: #444;
            transition: 0.3s;
            border-radius: 30px;
        }

        .slider:before {
            position: absolute;
            content: "";
            height: 24px;
            width: 24px;
            left: 3px;
            bottom: 3px;
            background-color: white;
            transition: 0.3s;
            border-radius: 50%;
        }

        input:checked + .slider {
            background-color: var(--success-color);
        }

        input:checked + .slider:before {
            transform: translateX(30px);
        }

        /* Form Elements */
        .form-group {
            margin-bottom: 20px;
        }

        .form-label {
            display: block;
            margin-bottom: 8px;
            font-weight: 600;
            color: var(--text-primary);
        }

        .form-input {
            width: 100%;
            padding: 12px 15px;
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 1rem;
            transition: all 0.3s ease;
        }

        .form-input:focus {
            outline: none;
            border-color: var(--primary-color);
            background: rgba(255, 255, 255, 0.15);
        }

        .form-select {
            width: 100%;
            padding: 12px 15px;
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid var(--border-color);
            border-radius: 8px;
            color: var(--text-primary);
            font-size: 1rem;
        }

        .form-select option {
            background: var(--dark-bg);
            color: var(--text-primary);
        }

        /* Buttons */
        .btn {
            padding: 12px 25px;
            border-radius: 8px;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            border: none;
            font-size: 1rem;
        }

        .btn-primary {
            background: linear-gradient(45deg, var(--primary-color), #2d4a7a);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(88, 101, 242, 0.3);
        }

        .btn-success {
            background: linear-gradient(45deg, var(--success-color), #2d5a2d);
            color: white;
        }

        .btn-success:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(67, 181, 129, 0.3);
        }

        .btn-warning {
            background: linear-gradient(45deg, var(--warning-color), #7a4a2d);
            color: white;
        }

        .btn-warning:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(250, 166, 26, 0.3);
        }

        /* Responsive */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
            }

            .sidebar.open {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }

            .settings-grid {
                grid-template-columns: 1fr;
            }
        }

        /* Toggle Button */
        .sidebar-toggle {
            display: none;
            position: fixed;
            top: 20px;
            left: 20px;
            z-index: 1001;
            background: var(--primary-color);
            border: none;
            color: white;
            padding: 10px;
            border-radius: 8px;
            cursor: pointer;
        }

        @media (max-width: 768px) {
            .sidebar-toggle {
                display: block;
            }
        }
    </style>
</head>
<body>
    <!-- Mobile Toggle Button -->
    <button class="sidebar-toggle" onclick="toggleSidebar()">
        <i class="fas fa-bars"></i>
    </button>

    <!-- Sidebar -->
    <div class="sidebar" id="sidebar">
        <div class="sidebar-header">
            <img src="<%= botAvatar %>" alt="Bot Avatar" class="bot-avatar">
            <div class="bot-name"><%= botUsername %></div>
            <div class="bot-status">
                <span id="guardStatus" class="badge <%= guardEnabled ? 'bg-success' : 'bg-danger' %>">
                    <%= guardEnabled ? 'Aktif' : 'Pasif' %>
                </span>
            </div>
        </div>
        
        <ul class="nav-menu">
            <li class="nav-item">
                <a href="/dashboard/<%= guildID %>" class="nav-link">
                    <i class="fas fa-tachometer-alt nav-icon"></i>
                    Dashboard
                </a>
            </li>
            <li class="nav-item">
                <a href="/guard/<%= guildID %>" class="nav-link">
                    <i class="fas fa-shield-halved nav-icon"></i>
                    Guard Ayarları
                </a>
            </li>
            <li class="nav-item">
                <a href="/logs/<%= guildID %>" class="nav-link">
                    <i class="fas fa-list-alt nav-icon"></i>
                    Log Kayıtları
                </a>
            </li>
            <li class="nav-item">
                <a href="/safe-users/<%= guildID %>" class="nav-link">
                    <i class="fas fa-user-shield nav-icon"></i>
                    Güvenli Kullanıcılar
                </a>
            </li>
            <li class="nav-item">
                <a href="/roles/<%= guildID %>" class="nav-link">
                    <i class="fas fa-user-tag nav-icon"></i>
                    Rol Yönetimi
                </a>
            </li>
            <li class="nav-item">
                <a href="/channels/<%= guildID %>" class="nav-link">
                    <i class="fas fa-hashtag nav-icon"></i>
                    Kanal Yönetimi
                </a>
            </li>
            <li class="nav-item">
                <a href="/bot-settings/<%= guildID %>" class="nav-link">
                    <i class="fas fa-robot nav-icon"></i>
                    Bot Ayarları
                </a>
            </li>
            <li class="nav-item">
                <a href="/settings/<%= guildID %>" class="nav-link active">
                    <i class="fas fa-cog nav-icon"></i>
                    Genel Ayarlar
                </a>
            </li>
        </ul>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <div class="header">
            <div class="header-left">
                <h1 class="page-title">Genel Ayarlar</h1>
                <div class="server-info">
                    <img src="<%= guild.iconURL({ size: 128, extension: 'png' }) || 'https://cdn.discordapp.com/embed/avatars/0.png' %>" alt="Server Icon" class="server-icon" onerror="this.src='https://cdn.discordapp.com/embed/avatars/0.png'">
                    <div class="server-details">
                        <h4><%= guild.name %></h4>
                        <p><%= guild.memberCount %> üye</p>
                    </div>
                </div>
            </div>
            <div class="user-actions">
                <a href="/logout" class="logout-btn">
                    <i class="fas fa-sign-out-alt"></i>
                    Çıkış Yap
                </a>
            </div>
        </div>

        <div class="settings-grid">
            <!-- Log Settings -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-icon logs">
                        <i class="fas fa-list-alt"></i>
                    </div>
                    <div class="card-title">Log Ayarları</div>
                </div>

                <form action="/settings/<%= guildID %>" method="POST">
                    <div class="form-group">
                        <label class="form-label">Log Kanalı</label>
                        <select class="form-select" name="logChannelID">
                            <option value="">Log kanalı seçin</option>
                            <% guild.channels.cache.filter(ch => ch.type === 0).forEach(channel => { %>
                                <option value="<%= channel.id %>" <%= logChannelID === channel.id ? 'selected' : '' %>>
                                    #<%= channel.name %>
                                </option>
                            <% }); %>
                        </select>
                    </div>

                    <div class="setting-item">
                        <div class="setting-info">
                            <div class="setting-label">Detaylı Loglar</div>
                            <div class="setting-description">Tüm aktiviteleri detaylı olarak logla</div>
                        </div>
                        <label class="toggle-switch">
                            <input type="checkbox" name="detailedLogs">
                            <span class="slider"></span>
                        </label>
                    </div>

                    <div class="setting-item">
                        <div class="setting-info">
                            <div class="setting-label">Log Temizleme</div>
                            <div class="setting-description">Eski logları otomatik temizle</div>
                        </div>
                        <label class="toggle-switch">
                            <input type="checkbox" name="autoCleanLogs">
                            <span class="slider"></span>
                        </label>
                    </div>

                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Kaydet
                    </button>
                </form>
            </div>

            <!-- Backup Settings -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-icon backup">
                        <i class="fas fa-download"></i>
                    </div>
                    <div class="card-title">Yedekleme Ayarları</div>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Otomatik Yedekleme</div>
                        <div class="setting-description">Sunucu ayarlarını otomatik yedekle</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="autoBackup">
                        <span class="slider"></span>
                    </label>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Yedekleme Sıklığı</div>
                        <div class="setting-description">Yedekleme aralığını belirle</div>
                    </div>
                    <select class="form-select" style="width: auto;">
                        <option value="daily">Günlük</option>
                        <option value="weekly">Haftalık</option>
                        <option value="monthly">Aylık</option>
                    </select>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Yedek Sayısı</div>
                        <div class="setting-description">Saklanacak yedek sayısı</div>
                    </div>
                    <select class="form-select" style="width: auto;">
                        <option value="5">5 yedek</option>
                        <option value="10">10 yedek</option>
                        <option value="20">20 yedek</option>
                    </select>
                </div>

                <button class="btn btn-success">
                    <i class="fas fa-download"></i> Manuel Yedek Al
                </button>
            </div>

            <!-- Security Settings -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-icon security">
                        <i class="fas fa-shield-alt"></i>
                    </div>
                    <div class="card-title">Güvenlik Ayarları</div>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">İki Faktörlü Doğrulama</div>
                        <div class="setting-description">Panel erişimi için 2FA gerektir</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="twoFactorAuth">
                        <span class="slider"></span>
                    </label>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">IP Kısıtlaması</div>
                        <div class="setting-description">Belirli IP'lerden erişime izin ver</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="ipRestriction">
                        <span class="slider"></span>
                    </label>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Oturum Süresi</div>
                        <div class="setting-description">Oturum zaman aşımı süresi</div>
                    </div>
                    <select class="form-select" style="width: auto;">
                        <option value="1">1 saat</option>
                        <option value="6">6 saat</option>
                        <option value="24">24 saat</option>
                        <option value="168">1 hafta</option>
                    </select>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Kanal Koruması</div>
                        <div class="setting-description">Sunucuda kanal oluşturma/silme işlemlerini korur</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="kanalKoruma" <%= panel && panel.kanalKoruma ? 'checked' : '' %>>
                        <span class="slider"></span>
                    </label>
                </div>
                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Rol Koruması</div>
                        <div class="setting-description">Sunucuda rol oluşturma/silme işlemlerini korur</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="rolKoruma" <%= panel && panel.rolKoruma ? 'checked' : '' %>>
                        <span class="slider"></span>
                    </label>
                </div>
                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Emoji Koruması</div>
                        <div class="setting-description">Sunucuda emoji ekleme/silme işlemlerini korur</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="emojiKoruma" <%= panel && panel.emojiKoruma ? 'checked' : '' %>>
                        <span class="slider"></span>
                    </label>
                </div>
                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Ban/Kick Koruması</div>
                        <div class="setting-description">Sunucuda toplu ban/kick işlemlerini korur</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="banKickKoruma" <%= panel && panel.banKickKoruma ? 'checked' : '' %>>
                        <span class="slider"></span>
                    </label>
                </div>

                <button class="btn btn-warning">
                    <i class="fas fa-key"></i> Şifre Değiştir
                </button>
            </div>

            <!-- Notification Settings -->
            <div class="settings-card">
                <div class="card-header">
                    <div class="card-icon notifications">
                        <i class="fas fa-bell"></i>
                    </div>
                    <div class="card-title">Bildirim Ayarları</div>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Discord Bildirimleri</div>
                        <div class="setting-description">Önemli olayları Discord'da bildir</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="discordNotifications">
                        <span class="slider"></span>
                    </label>
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">E-posta Bildirimleri</div>
                        <div class="setting-description">Kritik olayları e-posta ile bildir</div>
                    </div>
                    <label class="toggle-switch">
                        <input type="checkbox" name="emailNotifications">
                        <span class="slider"></span>
                    </label>
                </div>

                <div class="form-group">
                    <label class="form-label">E-posta Adresi</label>
                    <input type="email" class="form-input" placeholder="bildirim@example.com">
                </div>

                <div class="setting-item">
                    <div class="setting-info">
                        <div class="setting-label">Bildirim Sıklığı</div>
                        <div class="setting-description">Bildirim gönderme sıklığı</div>
                    </div>
                    <select class="form-select" style="width: auto;">
                        <option value="immediate">Anında</option>
                        <option value="hourly">Saatlik</option>
                        <option value="daily">Günlük</option>
                    </select>
                </div>

                <button class="btn btn-primary">
                    <i class="fas fa-save"></i> Kaydet
                </button>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    <script>
        function toggleSidebar() {
            const sidebar = document.getElementById('sidebar');
            sidebar.classList.toggle('open');
        }

        // Close sidebar when clicking outside on mobile
        document.addEventListener('click', function(event) {
            const sidebar = document.getElementById('sidebar');
            const sidebarToggle = document.querySelector('.sidebar-toggle');
            
            if (window.innerWidth <= 768) {
                if (!sidebar.contains(event.target) && !sidebarToggle.contains(event.target)) {
                    sidebar.classList.remove('open');
                }
            }
        });
    </script>
</body>
<%- include('layout-blocker.ejs') %>
</html> 